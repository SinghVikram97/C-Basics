#include <bits/stdc++.h>
using namespace std;
int main()
{
  // char arr[4];
  // cin.getline(arr, 1, '.'); // 1 for \0 so takes only 1 char as input , '.' as delimiter
  // cout << arr << endl;

  // string s;
  // getline(cin, s, '.'); // string with spaces
  // cout << s << endl;

  // 'a' is a character constant, it is an integer
  // "a" is a array of characters that contains one character and a \0
}