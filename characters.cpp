#include <bits/stdc++.h>
using namespace std;
int main()
{

  // char ch;
  // ch = getchar(); // takes only 1 character 1byte=8bits
  // putchar(ch);    // Outputs it
  // cout << endl;

  // int a; // can be used for int also (only 1 digit)
  // a = getchar();
  // putchar(a);
  // cout << endl;

  // int b; // Use getchar 3 times as getchar also takes in enter
  // b = getchar();
  // putchar(b);
  // cout << endl;

  // // Value of EOF in C is usually -1 so we take char as int
  // cout << EOF << endl;

  // A character b/w single quotes is just ascii value in machine
  // 'A' is 65
  // cout << (int)'a' << endl;
  // cout << (int)"a" << endl; // Error as it is const char *

  // cout << '9' - '8' << endl;       // 1
  // cout << 'a' + 'b' << endl;       // 195 ASCII value of 'a' is 97+98=195
  // cout << char('a' + 'b') << endl; // character with ascii value 195
  // char c = '2';
  // if (c >= '1' && c <= '9')  // Compares ascii value 46 >45
  // {
  //   cout << "True" << endl;
  // }
}